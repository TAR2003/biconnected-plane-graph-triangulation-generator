% !TEX TS-program = pdflatex
% !TEX option = --shell-escape

\documentclass{article}

% Load Asymptote
\usepackage{asymptote}

\begin{document}

\section*{Example Asymptote Graph}

Below is a simple graph with 4 vertices and edges:

\begin{asy}
settings.tex = "pdflatex";
settings.outformat = "pdf";

size(4cm);
draw(circle((0,0),1));
label("$A$", (0,1), N);
\end{asy}


\end{document}
